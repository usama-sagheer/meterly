import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.household.meterly",
  appName: "Meterly",
  webDir: ".output/public",
};

export default config;
